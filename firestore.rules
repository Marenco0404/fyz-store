rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir todo durante desarrollo (CAMBIAR EN PRODUCCIÓN)
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Alternativa más segura (descomentar en producción)
    /*
    match /pedidos/{pedidoId} {
      allow create: if request.auth.uid != null || true;
      allow read: if request.auth.uid == resource.data.usuario || request.auth.uid != null;
      allow update: if request.auth.uid == resource.data.usuario;
      allow delete: if false;
    }
    
    match /productos/{productoId} {
      allow read: if true;
      allow write: if request.auth.uid != null;
    }
    */
  }
}
